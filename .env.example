# PostgreSQL
POSTGRES_DB=booking_platform
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root

# OpenAI (GPT) — required for AI chatbot (leave empty for keyword-based fallback)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo